# Project Documentation

This repository uses a production-ready backend layout.

- `backend/` contains the Django app, configuration, templates, static assets, and environment example.
- `backend/.env.example` documents required runtime variables.
- `docs/` contains repository-level documentation.
- `deployment/` contains deployment-specific guidance and operational notes.
